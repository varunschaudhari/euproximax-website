# ============================================
# EuProximaX Website - Environment Variables
# ============================================
# Copy this file to .env and update with your actual values
# DO NOT commit .env file to version control

# ============================================
# API Configuration
# ============================================
# Backend API Base URL
# Production: https://api.euproximax.com/api/v1
# Development: http://localhost:3000/api/v1

VITE_API_BASE_URL=http://localhost:3000/api/v1

# ============================================
# Notes
# ============================================
# 1. Never commit .env file to version control
# 2. For local development, use: VITE_API_BASE_URL=http://localhost:3000/api/v1
# 3. For production, use: VITE_API_BASE_URL=https://api.euproximax.com/api/v1
# 4. Vite requires the VITE_ prefix for environment variables to be exposed to the client
# 5. After updating .env, restart the Vite dev server for changes to take effect
#
# ============================================
# PRODUCTION BUILD INSTRUCTIONS
# ============================================
# IMPORTANT: Vite embeds environment variables at BUILD TIME, not runtime!
#
# For Production:
# 1. Create .env file with production values (see above)
# 2. Run: npm run build
# 3. The dist/ folder will contain embedded environment variables
# 4. Deploy dist/ folder to your server
# 5. NO .env file needed on the server after build
#
# Alternative (build without .env file):
#   VITE_API_BASE_URL=https://api.euproximax.com/api/v1 npm run build
#
# See PRODUCTION_ENV_GUIDE.md for detailed instructions
